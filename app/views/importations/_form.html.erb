<%= form_with(model: importation) do |form| %>
  <% if importation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(importation.errors.count, "error") %> prohibited this importation from being saved:</h2>

      <ul>
        <% importation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
    <%= form.file_field(:content, accept: 'text/plain') %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
